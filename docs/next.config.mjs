import nextra from 'nextra'

const withNextra = nextra({
  // theme: 'nextra-theme-docs', // Not needed in v4
  // themeConfig: './theme.config.jsx' // Not needed in v4
})

export default withNextra({
  // Next.js config
})
