## API Report File for "@opensourceframework/next-session"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { IncomingMessage } from 'http';
import { ServerResponse } from 'http';

// Warning: (ae-forgotten-export) The symbol "SessionRecord" needs to be exported by the entry point session.d.ts
//
// @public (undocumented)
function nextSession<T extends SessionRecord = SessionRecord>(options?: Options): ((req: IncomingMessage, res: ServerResponse) => Promise<Session<T>>) & {
    getWebSession: (req: Request, res: Response | Headers) => Promise<Session<T>>;
};
export default nextSession;

// @public (undocumented)
export interface Options {
    // (undocumented)
    autoCommit?: boolean;
    // Warning: (ae-forgotten-export) The symbol "Cookie" needs to be exported by the entry point session.d.ts
    //
    // (undocumented)
    cookie?: Partial<Pick<Cookie, "maxAge" | "httpOnly" | "path" | "domain" | "secure" | "sameSite">>;
    // (undocumented)
    decode?: (encryptedSid: string) => string | null;
    // (undocumented)
    encode?: (rawSid: string) => string;
    // (undocumented)
    genid?: () => string;
    // (undocumented)
    name?: string;
    // (undocumented)
    store?: SessionStore;
    // (undocumented)
    touchAfter?: number | string;
}

// @public (undocumented)
export type Session<T extends SessionRecord = SessionRecord> = {
    id: string;
    touch(): void;
    commit(): Promise<void>;
    destroy(): Promise<void>;
    [isNew]?: boolean;
    [isTouched]?: boolean;
    [isDestroyed]?: boolean;
} & SessionData<T>;

// @public (undocumented)
export type SessionData<T = SessionRecord> = {
    cookie: Cookie;
} & T;

// @public (undocumented)
export interface SessionStore {
    // (undocumented)
    destroy(sid: string): Promise<void>;
    // (undocumented)
    get(sid: string): Promise<SessionData | null | undefined>;
    // (undocumented)
    set(sid: string, sess: SessionData): Promise<void>;
    // (undocumented)
    touch?(sid: string, sess: SessionData): Promise<void>;
}

// (No @packageDocumentation comment for this package)

```
